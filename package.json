{
  "author": {
    "email": "km.8k6ce+npm@gmail.com",
    "name": "kei-g"
  },
  "bugs": {
    "url": "https://github.com/kei-g/iterators/issues"
  },
  "description": "Iterators Library",
  "devDependencies": {
    "@biomejs/biome": "^2.5.4",
    "@types/node": "^26.1.1",
    "c8": "^11.0.0",
    "esbuild": "^0.28.1",
    "esbuild-register": "^3.6.0",
    "npm-run-all2": "^9.0.2",
    "rimraf": "^6.1.3",
    "typescript": "^6.0.3"
  },
  "engines": {
    "node": ">=14.17.5"
  },
  "homepage": "https://github.com/kei-g/iterators",
  "keywords": [
    "async",
    "asynciterable",
    "asynciterator",
    "iterable",
    "iterator"
  ],
  "license": "BSD-3-Clause",
  "main": "index.js",
  "name": "@kei-g/iterators",
  "overrides": {
    "c8": {
      "yargs": "^18.0.0"
    }
  },
  "publishConfig": {
    "access": "public"
  },
  "repository": "github:kei-g/iterators",
  "scripts": {
    "build": "npm-run-all2 --parallel clean lint --parallel build:bundle build:tsc",
    "build:bundle": "esbuild src/index.ts --bundle --minify --outfile=index.js --platform=node --target=ES2020",
    "build:tsc": "tsc",
    "clean": "rimraf coverage/ index.d.ts index.js lib/ types/",
    "cover": "c8 node --test",
    "lint": "biome lint --write",
    "postbuild:tsc": "tar -C types/src -cf - . | tar -xf - && rm -fr types",
    "postpublish": "npm run clean",
    "prepublishOnly": "npm run build",
    "test": "npm-run-all2 --parallel cover lint"
  },
  "type": "module",
  "types": "index.d.ts",
  "version": "1.1.14"
}
